apiVersion: 1

providers:
  - name: "OpenNotebook Dashboards"
    orgId: 1
    folder: "OpenNotebook"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
